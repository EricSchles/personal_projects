#if split_line[0] == '':
def determine_offset(line):
    split_line = line.split(' ')
    counter = 0
    while split_line[counter] == '':
        counter += 1
    return counter
        
def tokenize(line):
    
def main(code):
    
